# ---- core (required) ----------------------------------------------------
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
python-dotenv>=1.0.0
streamlit>=1.30.0
requests>=2.31.0

# ---- optional: better model / explanations -------------------------------
# The app runs without these. RandomForest is the fallback for xgboost, and a
# feature-importance heuristic is the fallback for shap.
# xgboost>=2.0.0
# shap>=0.44.0

# ---- optional: live broker feeds -----------------------------------------
# Only needed if DATA_PROVIDER is fyers or angelone.
# fyers-apiv3>=3.0.0
# smartapi-python>=1.4.0
# pyotp>=2.9.0

# ---- dev -----------------------------------------------------------------
pytest>=7.4.0
